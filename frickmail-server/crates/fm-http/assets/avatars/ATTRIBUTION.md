# Avatar service icon attribution

The PNG brand icons in `services/` are copied verbatim from the legacy
Avatars plugin image bundle (`plugins/avatars/images/services/` in this
repository).

```
The MIT License (MIT)

Copyright (c) 2016 RainLoop Team
```

They are baked into the Rust server so the production image needs no legacy
plugin files at runtime. When the legacy tree is removed (Phase 13), these
copies become the canonical set: keep this file with them.
